spring:
  application:
    name: auth-service

  # ── Data Stores ──
  data:
    redis:
      host: localhost
      port: 6379

  # ── MongoDB ──
  mongodb:
    uri: mongodb://localhost:27017/premisave-auth

  # ── Mail (Gmail SMTP) ──
  mail:
    host: smtp.gmail.com
    port: 587
    username: thepeacemakerske@gmail.com
    password: "nyea rbvz wkzp ksaz"
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true

  # ── File Upload Configuration ──
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB

# ── Server ──
server:
  port: 8080

# ── Application URLs ──
frontend:
  url: http://localhost:3000

backend:
  url: http://localhost:8080

# ── External Services ──
cloudinary:
  cloud-name: thepeacemakersllc
  api-key: 929659223288334
  api-secret: nAsBdgfXx86u7-Y8X2VnLa-2FbQ

# ── JWT ──
jwt:
  secret: eW91ci12ZXJ5LXNlY3VyZS1qd3Qtc2VjcmV0LWtleS0zMi1ieXRlcyE=
  expiration: 2592000000

# ── Rate Limiting ──
rate-limit:
  requests-per-minute: 20

# ── Email ──
email:
  support: ${SUPPORT_EMAIL:thepeacemakerske@gmail.com}

# ── Logging ──
logging:
  level:
    com.premisave.auth: DEBUG
    org.springframework.data.mongodb: DEBUG